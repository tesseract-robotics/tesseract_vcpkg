diff --git a/Extras/HACD/CMakeLists.txt b/Extras/HACD/CMakeLists.txt
index a91d117..2bf0bb2 100644
--- a/Extras/HACD/CMakeLists.txt
+++ b/Extras/HACD/CMakeLists.txt
@@ -34,9 +34,10 @@ IF (INSTALL_EXTRA_LIBS)
 		#FILES_MATCHING requires CMake 2.6
 		IF (${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION} GREATER 2.5)
 			IF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
-				INSTALL(TARGETS HACD DESTINATION .)
+				INSTALL(TARGETS HACD DESTINATION EXPORT HACDTargets DESTINATION .)
 			ELSE (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
 				INSTALL(TARGETS HACD
+                                        EXPORT HACDTargets
                                         RUNTIME DESTINATION bin
                                         LIBRARY DESTINATION lib${LIB_SUFFIX}
                                         ARCHIVE DESTINATION lib${LIB_SUFFIX})
@@ -51,5 +52,6 @@ IF (INSTALL_EXTRA_LIBS)
 			SET_TARGET_PROPERTIES(HACD PROPERTIES FRAMEWORK true)
 			SET_TARGET_PROPERTIES(HACD PROPERTIES PUBLIC_HEADER "${HACD_HDRS}")
 		ENDIF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
+		INSTALL(EXPORT HACDTargets DESTINATION ${BULLET_CONFIG_CMAKE_PATH})
 	ENDIF (NOT INTERNAL_CREATE_DISTRIBUTABLE_MSVC_PROJECTFILES)
 ENDIF (INSTALL_EXTRA_LIBS)
